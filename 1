#include<stdio.h>
int main()
{   printf("这是一个判断两数是否相等的程序\n输入两数用空格隔开，完成后点击回车\n若相等则输出为1\n若不相等则输出为0:\n"); 
    double a;
    double b;
    scanf("%lf%lf",&a,&b);
    bool x = a==b;
    printf("%d",x);
	return 0;
}
